// 全サブプロジェクト共通の設定
subprojects {
    apply plugin: 'java'

    /*
    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(8)
        }
    }
    */

    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }
}
